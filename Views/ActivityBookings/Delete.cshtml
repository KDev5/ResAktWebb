@model ResAktWebb.Models.ActivityBooking

@{
    ViewData["Title"] = "Delete";
    Layout = "_Layout_Admin";
}
<div class="my-div">

    <h1>Ta bort</h1>
    <div>
        <h4>ActivityBooking</h4>
        <hr />
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.CustomerName)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.CustomerName)
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.NumParticipants)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.NumParticipants)
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Activity)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Activity.Id)
            </dd>
        </dl>

        <form asp-action="Delete">
            <input type="hidden" asp-for="Id" />
            <input type="submit" value="Ta bort" class="btn btn-danger" /> |
            <a asp-action="Index">Tillbaka till lista</a>
        </form>
    </div>
</div>
